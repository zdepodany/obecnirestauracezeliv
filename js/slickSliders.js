// Slick Slider for quotes
$(document).ready(function () {
  $(".quoteSlider").slick({
    autoplay: true,
    autoplaySpeed: 5000,
    speed: 2000,
    arrows: false,
    draggable: false,
  });
});
